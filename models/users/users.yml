version: 2

models:
  - name: users

    config:
      contract:
        enforced: true

    columns:
      - name: user_id
        data_type: int
        constraints:
          - type: primary_key

      - name: name
        data_type: string
